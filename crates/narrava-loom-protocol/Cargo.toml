[package]
name = "narrava-loom-protocol"
version = "0.5.2"
edition = "2024"
description = "Owned, serializable and language-neutral Narrava Loom protocol"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
